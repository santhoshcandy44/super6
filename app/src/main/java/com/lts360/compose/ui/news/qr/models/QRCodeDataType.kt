package com.lts360.compose.ui.news.qr.models

// Enum for QRCodeDataType (e.g., URL, Event, Contact, etc.)
enum class QRCodeDataType {
    TEXT,
    URL,
    EVENT,
    CONTACT,
    V_CARD,
    PHONE,
    EMAIL
}